<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.datou.entity.mapper.UserMapper">
    <insert id="insert" parameterType="com.datou.entity.User">
        insert into user(username,password,phone,age) values(#{username,jdbcType=CHAR},#{password,jdbcType=CHAR},#{phone},#{age})
    </insert>

</mapper>
